import { Routes } from '@angular/router';
import { RegisterFormComponent } from '../register/components/register-form/register-form.component';

export const routes: Routes = [{ path: 'login', component: RegisterFormComponent}];
